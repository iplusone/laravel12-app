services:
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    ports:
      - 8881:80
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: password
    depends_on:
      - mysql
    networks:
      - sail
networks:
  sail:
    external: true
